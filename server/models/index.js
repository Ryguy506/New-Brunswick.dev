const User = require('./User'); // imports the User model from the ./User module.
const Project = require('./Project'); // imports the Project model from the ./Project module.

module.exports = { User, Project }; //  exports an object containing the User and Project models for use in other modules.